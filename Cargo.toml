[package]
name = "syn-kit"
version = "0.3.0"
edition = "2021"
description = "Runtime and testing utilities for syn-grammar"
license = "MIT OR Apache-2.0"

[dependencies]
syn = { version = "2.0", features = ["full", "extra-traits"] }
proc-macro2 = "1.0"
quote = "1.0"
